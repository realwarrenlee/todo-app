version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "Build phase completed"
run:
  command: node server.js
  network:
    port: 3000
    env: PORT
  env:
    - name: NODE_ENV
      value: production
    - name: AWS_REGION
      value: ap-southeast-1
